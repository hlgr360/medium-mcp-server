# Medium MCP Server - Environment Configuration Template
# Copy this file to .env and customize as needed
# Note: This server uses browser automation, NOT the deprecated Medium API

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================

# Session file location (default: medium-session.json in project root)
# SESSION_FILE_PATH=./medium-session.json

# =============================================================================
# BROWSER CONFIGURATION
# =============================================================================

# Force headless mode (true/false)
# Useful for server environments where GUI is not available
# Default: Auto-detects based on session validity
# FORCE_HEADLESS=false

# Browser executable path (optional)
# Leave empty to use bundled Chromium from Playwright
# BROWSER_PATH=/path/to/chrome

# =============================================================================
# TIMEOUT OVERRIDES (milliseconds)
# =============================================================================

# These override the default constants in browser-client.ts
# Uncomment and adjust based on your network speed and system performance

# Maximum time to wait for user to complete login (default: 300000 = 5 minutes)
# TIMEOUT_LOGIN=300000

# Maximum time to wait for page load (default: 60000 = 60 seconds)
# TIMEOUT_PAGE_LOAD=60000

# Short wait for UI elements to appear (default: 2000 = 2 seconds)
# TIMEOUT_SHORT_WAIT=2000

# Wait for dynamic content to load (default: 3000 = 3 seconds)
# TIMEOUT_CONTENT_WAIT=3000

# Wait for rich text editor to initialize (default: 15000 = 15 seconds)
# TIMEOUT_EDITOR_LOAD=15000

# Wait for network activity to settle (default: 10000 = 10 seconds)
# TIMEOUT_NETWORK_IDLE=10000

# =============================================================================
# VIEWPORT CONFIGURATION
# =============================================================================

# Browser viewport dimensions
# VIEWPORT_WIDTH=1280
# VIEWPORT_HEIGHT=720

# =============================================================================
# LOGGING & DEBUGGING
# =============================================================================

# Enable verbose logging (true/false)
# VERBOSE_LOGGING=false

# Save screenshots on error (true/false)
# SCREENSHOT_ON_ERROR=true

# Screenshot directory
# SCREENSHOT_DIR=./screenshots

# =============================================================================
# DEVELOPMENT OPTIONS
# =============================================================================

# Enable development mode (keeps browser open after operations)
# DEV_MODE=false

# Slow down automation for debugging (milliseconds to wait between actions)
# SLOW_MO=0

# =============================================================================
# NOTES
# =============================================================================
#
# 1. This file is a template. Copy it to .env to use it.
# 2. .env file is gitignored for security (contains your local settings)
# 3. Most users don't need to configure anything - defaults work well
# 4. Only customize if you experience timeouts or need specific browser settings
# 5. After changing .env, restart the MCP server for changes to take effect
#
# IMPORTANT: This server uses BROWSER AUTOMATION, not the deprecated Medium API.
# No API keys or OAuth credentials are needed. The server uses your Medium login session.
#
